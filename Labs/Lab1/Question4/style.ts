import {StyleSheet} from 'react-native';

const STYLES = StyleSheet.create({
  containter: {backgroundColor: '#fff'},
  box: {
    width: 100,
    height: 100,
    justifyContent: 'center',
    alignItems: 'center',
    margin: 20,
  },
});

export default STYLES;
